.container-fluid
  h2.col-lg-12
    a  href="/jurisdictions/{{jurisdiction.id}}"
      | {{jurisdiction.name}}
    | &nbsp;/&nbsp;
    a  ui-sref="proposedLaw.nodes({proposedLawId:proposedLaw.id,tree:''})"
      | {{proposedLaw.title}}
  div ui-view=true
