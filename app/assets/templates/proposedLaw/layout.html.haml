.container-fluid
  %h2.col-lg-12
    %a{ href: "/jurisdictions/{{jurisdiction.id}}" }= "{{jurisdiction.name}}"
    \/
    %a{ ui: { sref: 'proposedLaw.browse({proposedLawId:proposedLaw.id,tree:\'\'})' } }= "{{proposedLaw.title}}"
  %div{ ui: { view: true } }
